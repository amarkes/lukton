// src/components/ProductChanges.js
import { Outlet } from 'react-router-dom';

import React from 'react';

const ProductChanges = () => {
  return (
    <Outlet />
  );
};


export default ProductChanges;
